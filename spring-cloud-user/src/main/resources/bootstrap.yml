spring:
  application:
    name: springclouduser
  cloud:
    config:
      uri: http://localhost:8080
      fail-fast: true
      username: user
      password: ${CONFIG_SERVER_PASSWORD:password}
      profile: dev
      label: master

